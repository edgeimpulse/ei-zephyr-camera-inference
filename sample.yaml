sample:
  description: Run an Edge Impulse image model on a camera supported by the
    Zephyr video API
  name: ei-zephyr-camera-inference
common:
    tags: introduction
    harness: console
    harness_config:
      type: one_line
      regex:
        - "Edge Impulse camera inferencing on Zephyr(.*)"
